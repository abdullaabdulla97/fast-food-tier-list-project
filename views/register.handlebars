<!DOCTYPE html>
<html lang="en">
<head>
    <title>{{title}}</title> {{!--dynamic title--}}
    <link rel="stylesheet" href="/styles/styles.css"> {{!--links our css file--}}
    <link rel="icon" type="image/png" href="/favicon-register.png"> {{!--Links the favicon image--}}
</head>
<body class="register-page"> {{!--applies the register-page class for styling--}}
    <div class="register-card"> {{!--container for the registration form--}}
        <h1 class="register-title">{{title}}</h1> {{!--dynamic title--}}

        <div class="form-group"> {{!--container for username input--}}
            <label for="username" class="form-label">Username</label> {{!--label for username input--}}
            <input id="username" class="form-input" autocomplete="username"/> {{!--username input field--}}
        </div>

        <div class="form-group"> {{!--container for password input--}}
            <label for="password" class="form-label">Password</label> {{!--label for password input--}}
            <input id="password" class="form-input" type="password" autocomplete="new-password"/> {{!--password input field--}}
        </div>

        <button class="btn" id="submitBtn">Create Account</button> {{!--submit button--}}
        <div id="status" class="register-msg" role="status"></div> {{!--status message area--}}

        <p class="register-footer">Already have an account? <a href="/ranklist">Go to the Tier List Page</a></p> {{!--link to the ranklist page--}}
    </div>
    <script src="/js/script.js"></script> {{!--links our JavaScript file--}}
</body>
</html>
