<!DOCTYPE html>
<html lang="en">
<head>
    <title>{{title}}</title>
    <link rel="stylesheet" href="/styles/styles.css"> {{!--links our css file--}}
    <link rel="icon" type="image/png" href="/favicon-register.png"> {{!--Links the favicon image--}}
</head>
<body class="register-page">
    <div class="register-card">
        <h1 class="register-title">{{title}}</h1>

        <div class="form-group">
            <label for="username" class="form-label">Username</label>
            <input id="username" class="form-input" autocomplete="username"/>
        </div>

        <div class="form-group">
            <label for="password" class="form-label">Password</label>
            <input id="password" class="form-input" type="password" autocomplete="new-password"/>
        </div>

        <button class="btn" id="submitBtn">Create Account</button>
        <div id="status" class="register-msg" role="status"></div>

        <p class="register-footer">Already have an account? <a href="/ranklist">Go to the Tier List Page</a></p>
    </div>
    <script src="/js/script.js"></script>
</body>
</html>